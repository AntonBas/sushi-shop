global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'sushi-backend'
    metrics_path: '/actuator/prometheus'
    basic_auth:
      username: ${PROMETHEUS_USER}
      password: ${PROMETHEUS_PASSWORD}
    static_configs:
      - targets: ['backend:8080']